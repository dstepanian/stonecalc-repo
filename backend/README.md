# Backend (server)

1. cd backend
2. cp .env.example .env and edit if needed
3. npm install
4. npm run dev (requires nodemon) or npm start

The server exposes:
- POST /api/calc (JSON body — see frontend for example)
- GET /api/history
